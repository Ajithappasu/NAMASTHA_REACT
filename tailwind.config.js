/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{html,js,ts,jsx,tsx}"], // we are teling the tailwind that we can use tailwind config in all type of files in src folder
  theme: {
    extend: {},
  },
  plugins: [],
}

