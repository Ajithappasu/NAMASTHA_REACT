1)  // to write style css inside   js
  a) wrap it like an object in   style block 
  <div className="res-card" style={{
    backgroundColor:"black"
  }}>

  b)create a seperate css object and  keep it inside the style brackets {}.
  const jsCssObject ={
  backgroundColor:"black",
  flex:"inline"
    }
 <div className="res-card" style={jsCssObject}>
  note: this style is only valid for the div block which it is present in .

2_props: 
 these are like dynamic   parmeters passed as the object    

ex:
  <RestarentCard resName ="Paradise" cousine ="Indian"  rating ="3.0"  time ="50"/>
   // we use this in function  const { resName, cousine,  rating,time } =props;
  // we can use props.resName, directyl id needed